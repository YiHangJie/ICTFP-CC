from Benchmark_read_attack import Benchmark_generation
from BETA_read_attack import Beta_generation

if __name__ == "__main__":
    # Benchmark_generation(noise_amp=0.1)
    # Benchmark_generation(noise_amp=0.2)
    # Benchmark_generation(noise_amp=0.3)
    Beta_generation(noise_amp=0.1)
    Beta_generation(noise_amp=0.2)
    Beta_generation(noise_amp=0.3)